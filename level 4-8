#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Ededi daxil edin: ";
    cin >> n;
    string bin = "";
    if (n == 0) bin = "0";
    while (n > 0) {
        bin = char(n % 2 + '0') + bin;
        n /= 2;
    }
    cout << "Binary forma: " << bin;
    return 0;
}
