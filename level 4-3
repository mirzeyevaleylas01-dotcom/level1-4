#include <iostream>
using namespace std;

bool sade(int n) {
    if (n < 2) return false;
    for (int i = 2; i * i <= n; i++)
        if (n % i == 0) return false;
    return true;
}

int main() {
    int n;
    cout << "N daxil edin: ";
    cin >> n;
    for (int i = 2; i < n; i++)
        if (sade(i)) cout << i << " ";
    return 0;
}
