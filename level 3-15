#include <iostream>
using namespace std;

int main() {
    int n, x, sum = 0;
    cout << "Nece eded daxil edeceksiniz? ";
    cin >> n;
    for(int i=1; i<=n; i++) {
        cout << i << ". ededi daxil edin: ";
        cin >> x;
        sum += x;
    }
    cout << "Cem: " << sum;
    return 0;
}
