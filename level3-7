#include <iostream>
using namespace std;

int main() {
    int n, sum = 0;
    cout << "Ededleri daxil edin (0 bitirir): ";
    while(true) {
        cin >> n;
        if(n == 0) break;
        sum += n;
    }
    cout << "Cem: " << sum;
    return 0;
}
