#include <iostream>
using namespace std;

int main() {
    int n, i, sadedir = 1;
    cout << "Ededi daxil edin: ";
    cin >> n;
    if(n < 2) sadedir = 0;
    else {
        for(i = 2; i <= n/2; i++)
            if(n % i == 0) {
                sadedir = 0;
                break;
            }
    }
    if(sadedir) cout << "Sade ededdir";
    else cout << "Murekkeb ededdir";
    return 0;
}
