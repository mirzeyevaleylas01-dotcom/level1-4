#include <iostream>
#include <cmath>
using namespace std;

int main() {
    for (int i = 100; i < 1000; i++) {
        int a = i / 100, b = (i / 10) % 10, c = i % 10;
        if (pow(a, 3) + pow(b, 3) + pow(c, 3) == i)
            cout << i << " ";
    }
    return 0;
}
